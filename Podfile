# Uncomment the next line to define a global platform for your project
 platform :ios, '17.0'

target 'iOS _Photo Cropper' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS _Photo Cropper
    pod 'Alamofire', '~> 4.9.0'
  target 'iOS _Photo CropperTests' do
    inherit! :search_paths
    pod 'Alamofire', '~> 4.9.0'
  end

  target 'iOS _Photo CropperUITests' do
    pod 'Alamofire', '~> 4.9.0'
  end

end
